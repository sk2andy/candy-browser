# Tabs and profiles

## Topic lookup

| Need | Detail | Main code |
| --- | --- | --- |
| Tab state, ordering, retention, Gecko session residency, persistence | [`lifecycle-and-persistence.md`](lifecycle-and-persistence.md) | `BrowserTab`, `BrowserSessionResidencyRules` (engine-neutral policy), `data/Tab*Rules`, `BrowserSessionStore` |
| Previews, snoozing, profiles, Gecko/WebKit context isolation | [`previews-snooze-and-isolation.md`](previews-snooze-and-isolation.md) | `TabPreview*`, `SnoozedTab*`, `BrowserProfileRules`, `GeckoProfileStorageRules`, `WKWebsiteDataStore` adapter |
| Sync profile binding and writable remote device profiles | [`../sync/app-integration.md`](../sync/app-integration.md) | The current device binds to one persisted local profile; remote runtime projections reuse native engine interaction but never enter `BrowserSessionStore` |
| Launcher app shortcuts | Long-pressing Candy exposes two static actions for new regular/private tabs plus up to two dynamic recent inactive local profiles within the launcher's device-specific limit. Shortcut actions use compact black glyphs on light neutral adaptive-icon surfaces. All actions pass through a no-history dispatcher so warm launches preserve private in-memory tabs. Recent-profile ranking uses only regular-tab activity, so private use never changes launcher metadata; synced runtime profiles are not published. | `LauncherShortcutActivity`, `browser/integration/LauncherShortcutRules`, `LauncherShortcutPublisher`, `LauncherShortcutIntentHandler` |
| Home-screen search widget | A horizontally resizable 4×1 widget collapses to an icon-only 2×1 layout. Candy opens without changing browser state; the search area opens a regular tab in the current profile; the private action opens a memory-only private tab in the current local profile; and the first two local profile icons each open a regular tab in that profile. Profile controls disappear when profiles are disabled. The widget has no settings surface. | `CandySearchWidgetProvider`, `browser/integration/CandySearchWidgetRules`, `LauncherShortcutIntentHandler` |
| Per-profile wallpapers | Long-pressing a local profile exposes separate editors for the new-tab wallpaper and tab-switcher wallpaper. Android's document picker imports each image into Candy-owned storage; every slot persists its own bounded zoom and normalized crop offsets. Regular blank tabs and their synthesized previews use the new-tab image, while the complete tab overview uses the switcher image with a contrast scrim. Private tabs and synced profiles never render either image. Legacy single-wallpaper profiles migrate that image into both slots. | `browser/ProfileWallpaperRules`, `data/ProfileWallpaperStore`, `ProfileWallpaperEditorActivity`, `ui/ProfileWallpaperEditorScreen`, `ui/BrowserScreen` |
| Candy Stacks | Manual profile-bound tab groups with name, color, chosen preview, inline expand/collapse marker, layered collapsed card, trigger-anchored stacking/fanning, and a separately configurable Coverflow, Grid, or List folder view. The shared `browser/TabStack` model and `data/TabStackRules` own platform-free identity, validation, membership, collapse, preview, visibility, and persistence-boundary decisions. Android owns the controller, persistence adapter, and stack UI. The tapped marker tab owns the collapsed position while the configured preview independently supplies its cover. During Coverflow folding, a bounded decorative motion layer brings up to six off-screen stack members in from the visible edge without composing every intervening pager page. Stack folders cap their width on large screens, use two Grid columns on compact widths and three on tablet widths, and expose color choices as accessible radio controls with 48-dp targets. Coverflow and Grid are stack-aware; List remains flat. | shared `browser/TabStack`, shared `data/TabStackRules`; Android `BrowserController`, `BrowserSessionStore`, `ui/TabStackUi`, `ui/TabStackFolderLayoutRules`, `ui/TabStackMotionRules`, `ui/TabOverview` |
| Tab overview modes, ordering, hero layout and gestures | By default every overview mode reveals the selected tab whenever the overview opens, independent of retained scroll state. List and grid modes can instead start at the newest end and bottom-align short layouts; incomplete grid rows are leading-padded so the newest tab stays bottom-right. Optional recent-use sorting keeps pins first, places the least recently used tabs before the newest tabs, and disables manual reorder. When pins exist outside the current viewport, a fading edge affordance beside the bottom chrome scrolls back to them. A matching subtle settings affordance on the opposite edge opens Settings without closing the overview, so Back returns to the switcher. Both edge affordances keep their full 48dp hit targets inside the overview and use circular ripples without button pills. Both tab-action surfaces keep favorite and pin actions side by side. The tab-switcher overflow menu can close every deletable tab in the active profile while preserving pinned tabs. Their menus grow to 400dp where space allows and stay inside 12dp screen margins; the main browser menu wraps favorite and pin into a second toolbar row below 340dp, while the tab-card menu uses compact vertical toolbar content. Frosted tab-card menus use the overview itself as their live blur source. The overview captures system-bar insets before entering that nested blur composition, keeping profile and action chrome outside cutouts and navigation bars. Hero and grid previews stay compact in portrait and switch to 16:10 in landscape. Tablet-width grids use three columns; smaller grids use two. Hero cards remain height-bounded with room for neighbors. Grid cards place the favicon/title pill and floating close action over the preview; pinned cards keep the pin in the pill and omit close. Selected grid cards retain a primary outline. Grid neighbors fade with the shared entry progress, while the selected preview keeps one continuous crop through the hero-to-card handoff. The pager draw area extends behind profile chrome, so upward dismiss drags remain visible up to the system-bar boundary; profile chrome stays above that overflow for drawing and touch input. Hero pager count, keys, and content share one captured per-composition tab snapshot while tabs are added or removed, preventing stale-index crashes and preview swaps; dismissal retains that snapshot until its animation ends. Profile controls remain interactive while the entry hero blocks tab-content touches. The production profile switcher is now a shared Compose component used by Android and iOS; iOS keeps newly added local profiles and their tab assignment in session memory. Address chrome stays above both entry and exit hero animations. A parked address pill follows the shared position-and-size spring into centered overview chrome and back without snapping between anchors. | `ui/TabOverview.kt`, `shared/ui/ProfileSwitcher.kt`, `ui/TabOverviewHero.kt`, `ui/TabReorderUi.kt`, `ui/CompactTabGrid.kt`, `ui/CompactTabList.kt`, `ui/TabActionsFloatingMenu.kt`, `data/TabAutoSortingRules.kt`, `ui/TabOverviewHeroRules.kt`, `ui/TabOverviewGridRules.kt`, `ui/TabDismissPhysics.kt`, `ui/AddressBarMotion.kt` |

## Test lookup

| Surface | Tests |
| --- | --- |
| Pure tab rules | `data/Tab*Test`, `BrowserTabTest`, `BrowserSessionResidencyRulesTest` |
| Profiles and storage assignment | `BrowserProfileRulesTest`, `BrowserTabsControllerTest`, `BrowserSessionControllerTest`, `GeckoProfileStorageRulesTest`, `BrowserControllerProfilesInstrumentedTest`, `ProfileCreationFlowInstrumentedTest` |
| Profile wallpapers | `ProfileWallpaperRulesTest`, `ProfileWallpaperStoreInstrumentedTest`, `BrowserControllerProfileWallpaperInstrumentedTest`, `ProfileWallpaperEditorContractInstrumentedTest`, `ProfileWallpaperEditorScreenInstrumentedTest` |
| Previews | `TabPreview*Test`, `TabPreviewRefreshInstrumentedTest` |
| Snoozing | `Snooze*Test`, `Snooze*InstrumentedTest` |
| Launcher app shortcuts | `LauncherShortcutRulesTest`, `LauncherShortcutInstrumentedTest` |
| Home-screen search widget | `CandySearchWidgetRulesTest`, `LauncherShortcutInstrumentedTest` |
| Tab overview layout and reorder | `TabOverviewReorderInstrumentedTest` |
| Candy Stacks shared rules, Android controller, persistence adapter and UI | common `TabStackRulesTest`; Android `BrowserControllerTabStackInstrumentedTest`, `BrowserSessionStoreInstrumentedTest`, `TabStackUiInstrumentedTest` |
