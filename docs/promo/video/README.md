# Candy Browser widescreen showcase

| Asset | Purpose |
| --- | --- |
| `candy-browser-showcase-16x9.mp4` | 30.4-second widescreen showcase at 1920 × 1080 and 60 fps |
| `candy-browser-showcase-16x9-poster.jpg` | Firefox extensions and Android engine-choice upload poster and fallback thumbnail |
| `candy-browser-theme.wav` | Original procedural 112 BPM future-tech theme used by the cut |
| `source/tab-switcher-live.mp4` | API 36.1 emulator recording: swipe-up into the live tab switcher |
| `source/link-peek-live.mp4` | API 36.1 emulator recording: Link Peek preview and `+` tab commit |
| `source/spoilerfree-sports-live.mp4` | Mobile capture using Spoilerfree Sports v1.2.0: scores hidden, revealed and hidden again |
| `source/hacker-news-comfort-live.mp4` | Mobile capture using Hacker News Comfort v1.1.0: classic list transformed into readable cards |
| `source/privacy-xray-live.mp4` | API 36.1 emulator recording: current Privacy X-Ray sheet with live block data |
| `source/privacy-xray-demo.html` | Deterministic local fixture available for future Privacy X-Ray recaptures |

Regenerate the video from the repository-owned emulator captures:

```bash
python3 scripts/generate_reddit_promo.py
```

The generator requires Pillow and FFmpeg with `libx264` and AAC support. With the same fonts and toolchain,
rendering is deterministic: scene timing, motion, copy, synthesizer notes and pseudo-random percussion use
fixed inputs and seed. `--output`, `--theme` and `--poster` can isolate all generated files for test renders.
The original 112 BPM future-tech theme is generated by the script and does not use third-party samples.

Kinetic text, controlled scale-ins, focus-driven camera pans and alternating diagonal glow wipes connect the
scenes. The gesture-first opener presents Candy's broader interaction concept instead of one isolated swipe.
The Android engine choice follows as the third scene, contrasting GeckoView with System WebView and showing
the two bundled default Firefox extensions; the poster uses this view. Cross-device sync follows with E2EE in
its self-hosted status card. Link Peek and Privacy X-Ray use live app footage.
The Topping captures use the exact published Topping scripts against deterministic mobile fixtures, which
keeps the score-toggle and Hacker News transformation reproducible. Reader Mode and Candy Trails use
repository screenshots.

The 16:9 H.264/AAC output is intended for landscape product pages, social previews and presentations.
Important copy is burned into the video so the message remains understandable when playback starts muted.
